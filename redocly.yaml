apis:
  sample@v1:
    root: docs/openapi.yaml
extends:
  - recommended
rules:
  no-used-components: error
theme:
  htmlTemplate: ./docs/index.htmlTemplate
  colors:
    primary:
      main: '#32329f'
  generateCodeSamples:
    laguages:
      - lang: curl
      - lang: Node.js
      - lang: JavaScript
